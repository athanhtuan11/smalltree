# Minimal requirements for SmallTree Academy (for slower servers)
# Core Flask Framework (REQUIRED - install first)
Flask==2.0.3
Flask-WTF==0.15.1
Flask-SQLAlchemy==2.5.1
Flask-Migrate==3.1.0
Jinja2==3.0.3
Werkzeug==2.0.3

# Production Server (REQUIRED)
gunicorn==20.1.0

# Environment Variables (REQUIRED)
python-dotenv==0.19.2

# Form Validation (REQUIRED)
email_validator==1.3.1
WTForms==3.0.1

# Optional packages (install manually if needed):
# python-docx==1.0.1     # Word document generation
# openpyxl==3.1.3        # Excel file handling  
# Pillow                 # Image processing (may fail on some systems)
# WeasyPrint             # PDF generation (may fail on some systems)

# AI Libraries (install manually if needed):
# google-generativeai>=0.3.0,<0.6.0  # Gemini AI
# cohere>=4.0.0,<5.0.0               # Cohere AI 
# openai>=0.28.0,<1.0.0              # OpenAI GPT-4/3.5
# requests>=2.28.0,<3.0.0           # For HTTP API calls
# groq>=0.4.0,<1.0.0                # Groq AI SDK

# AI Libraries (Optional - install manually if needed)
# google-generativeai
# openai
# groq
# cohere

# Image/PDF Processing (Optional - may fail on some systems)
# Pillow
# WeasyPrint

# Development/Debug only
# python-dotenv
